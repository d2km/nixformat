(library
 (name nix)
 (libraries menhirLib))

(menhir
 (modules parser)
 (flags "--explain" "--dump" "--strict" "--table"
        "--external-tokens" "Tokens"
        "--unused-token" "SCOMMENT"
        "--unused-token" "MCOMMENT")
 (infer true))

(ocamllex
 (modules lexer))
